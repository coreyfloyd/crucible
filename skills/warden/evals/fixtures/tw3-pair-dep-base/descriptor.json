{
  "fixture_id": "tw3-pair-dep-base",
  "obligation": "INV-P7 monotonicity (dep pair, BASE) — standalone: single non-dependency code file SKIPS via block-4; pairs with tw3-pair-dep-signal which flips to RUN by the dependency-glob signal alone (same 1-file count)",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 1 changed file src/util.py (non-dependency code) — block-1 miss, block-2 miss, block-3 miss (1 file), block-4 SKIP",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "NOT RUN — condition-skipped (block-4 fall-through, single non-escalator code file)"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
