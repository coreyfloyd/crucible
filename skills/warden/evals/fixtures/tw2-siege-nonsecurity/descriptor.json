{
  "fixture_id": "tw2-siege-nonsecurity",
  "obligation": "T-W2 — siege is SKIPPED on a non-security diff (conditional trigger; the security-diff/siege-present branch is covered by tw6-clean-pass)",
  "reviewer_set_mode": "full",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security-surface, multi-file (pure refactor of a formatting util — no auth/crypto/network/secret surface, so build's Step-5.5 siege trigger does NOT fire)",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
