{
  "fixture_id": "tw3-pair-dep-signal",
  "obligation": "INV-P7 monotonicity (dep pair, SIGNAL) — standalone: single file matching **/Cargo.toml RUNS via block-1; pairs with tw3-pair-dep-base (same 1-file count, SKIP) so only the dependency-glob signal flips skip→run",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 1 changed file crates/app/Cargo.toml — a non-root path that unambiguously matches **/Cargo.toml for a strict live matcher; block-1 dependency escalator RUN",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 1, dependency/lockfile escalator) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
