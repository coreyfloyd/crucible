{
  "fixture_id": "tw3-single-file-api",
  "obligation": "INV-P10 API escalation — standalone: inquisitor RUNS on a single file matching the INTERFACE/API/SCHEMA glob (**/api/**), where today's crude >1-file clause would have SKIPPED",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 1 changed file src/api/util.py matching **/api/** (api/ is a directory segment) — block-1 interface escalator RUN",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 1, interface/API escalator) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
