{
  "fixture_id": "tw6-clean-pass",
  "obligation": "T-W6 (clean diff → PASS) + T-W2/T-W8 (siege present on a security diff, inquisitor unconditional in full) + T-W5 (marker shape)",
  "reviewer_set_mode": "full",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "security-surface, multi-file (touches an auth/session module) so siege's Step-5.5 trigger fires; >1 file so it is unambiguously not a single-file diff",
  "per_leg_verdict_vector": {
    "temper": "clean — no {CONFIRMED,PLAUSIBLE}×{Critical,Important} merge finding",
    "delve": "clean — no kept {CONFIRMED,PLAUSIBLE}×{Critical,Important} finding",
    "red-team": "quality-gate verdict PASS (0 Fatal, 0 Significant)",
    "siege": "clean — 0 Critical, 0 High",
    "inquisitor": "all adversarial tests PASS"
  },
  "_note": "This descriptor is the OPERATOR INPUT for the live /warden run. The scorer never reads this file; the expected outcome in ground-truth.json is authored independently by hand from skills/warden/SKILL.md."
}
