{
  "_provenance": "The `expected` outcome below was authored BY HAND by applying skills/warden/SKILL.md's rules to the descriptor's diff shape + per-leg vector (see provenance.md for the per-field derivation citing the SKILL.md section). It is NOT copied from any live /warden run being scored. The scorer (_scorer.py) is a pure field comparator and never derives these values from the per-leg vector.",
  "fixture_id": "tw6-clean-pass",
  "expected": {
    "reviewer_set": ["temper", "delve", "red-team", "siege", "inquisitor"],
    "verdict": "PASS",
    "marker": {
      "aggregate_marker_count": 1,
      "aggregate_pipeline_id_source": "caller",
      "aggregate_build_tagged": true,
      "redteam_leg_marker_pipeline_id_source": "warden",
      "redteam_leg_marker_build_tagged": false
    },
    "leg_commit_subjects": []
  }
}
