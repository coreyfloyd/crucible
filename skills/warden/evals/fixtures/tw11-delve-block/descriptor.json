{
  "fixture_id": "tw11-delve-block",
  "obligation": "T-W11 — the delve leg BLOCKs with a named user hand-off when a finding is surfaced-not-applied (ambiguous repair) OR the native predicate still trips after the ≤2-re-run cap. This fixture: a surfaced-not-applied finding.",
  "reviewer_set_mode": "full",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, multi-file — delve keeps a {PLAUSIBLE}×{Critical} finding whose repair is ambiguous (`delve --fix` surfaces rather than applies it)",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "keeps a PLAUSIBLE@Critical finding; `delve --fix` SURFACES-not-applies it (ambiguous repair, no safe auto-fix) → predicate still trips",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
