{
  "fixture_id": "tw3-doc-nested-depth",
  "obligation": "INV-P8 any-depth match (M1) — standalone: inquisitor SKIPPED when EVERY changed path is a nested pure-doc file, exercising the gitignore-style any-depth PURE-DOC match",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 2 changed files, BOTH nested .md (sub/a.md, sub/b.md), no interface/dependency/binary path — block-2 all-doc skip via any-depth match",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "NOT RUN — condition-skipped (pure-doc-only diff, any-depth match, standalone set)"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
