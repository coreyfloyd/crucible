{
  "fixture_id": "tw3-single-file-dep",
  "obligation": "INV-P10 dependency/supply-chain escalation — standalone: inquisitor RUNS on a single file matching the DEPENDENCY/lockfile glob (**/package.json), where today's crude >1-file clause would have SKIPPED",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 1 changed file web/package.json (nested, unambiguously matches **/package.json) — block-1 dependency escalator RUN",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 1, dependency/lockfile escalator) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
