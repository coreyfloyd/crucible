{
  "fixture_id": "tw8-full-singlefile",
  "obligation": "T-W8 — in the FULL reviewer-set, inquisitor runs even on a single-file diff (unconditional; no regression of build Phase 4 Step 4 coverage)",
  "reviewer_set_mode": "full",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, single-file — identical single-file shape to tw3-standalone-singlefile, but the reviewer-set is `full`",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (unconditional in full) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
