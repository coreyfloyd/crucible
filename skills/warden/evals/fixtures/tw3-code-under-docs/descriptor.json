{
  "fixture_id": "tw3-code-under-docs",
  "obligation": "INV-P8 code-under-docs exclusion — standalone: a .py file under docs/ defeats the pure-doc block-2 (not every path is pure-doc), so inquisitor RUNS via block-3 (>1 file) — NOT via an escalator",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 2 changed files docs/conf.py + docs/guide.md — docs/conf.py is code (not pure-doc), so block-2 all-doc fails; no escalator matches either path; block-3 (>1 file) RUN",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 3, >1 file — the .py sibling defeats block-2 all-doc, NOT an escalator) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
