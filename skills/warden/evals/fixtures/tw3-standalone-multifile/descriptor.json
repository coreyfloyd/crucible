{
  "fixture_id": "tw3-standalone-multifile",
  "obligation": "T-W3 / INV-P3 — in the STANDALONE reviewer-set, inquisitor RUNS via block 3 of the risk-aware predicate (>1 changed file OR >1 top-level module) after blocks 1 (no escalator) and 2 (not all pure-doc) miss",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, multi-file code — 3 changed files across 2 top-level modules, no interface/dependency/binary path, not all pure-doc; block-1 miss, block-2 miss, block-3 RUN (>1 file OR >1 module)",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 3, >1 file) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
