{
  "fixture_id": "tw3-mixed-binary-text",
  "obligation": "INV-P9 binary/unparseable fail-safe — standalone: a diff containing a binary path RUNS inquisitor (note: in every constructible reachable case this is subsumed by block-3 >1 file; belt-and-suspenders)",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 2 changed files — 1 binary (assets/logo.png) + 1 text (src/util.py); block-1 binary/unparseable clause matches, and block-3 (>1 file) would independently RUN — so RUN either way",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 3 >1 file; block-1 binary clause also matches but is subsumed) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
