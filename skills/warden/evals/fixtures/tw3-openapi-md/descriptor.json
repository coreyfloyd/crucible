{
  "fixture_id": "tw3-openapi-md",
  "obligation": "F1 — escalator dominates the pure-doc branch (M4): a single openapi.md matches the openapi* interface glob, so inquisitor RUNS even though the path is a .md file",
  "reviewer_set_mode": "standalone",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, 1 changed file openapi.md — matches openapi* (INTERFACE/API/SCHEMA glob); block-1 escalator evaluated FIRST, so it RUNS before the pure-doc block-2 could skip it",
  "per_leg_verdict_vector": {
    "temper": "clean",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "RUNS (block 1, openapi* interface escalator dominates docs) — all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
