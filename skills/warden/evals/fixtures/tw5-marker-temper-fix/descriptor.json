{
  "fixture_id": "tw5-marker-temper-fix",
  "obligation": "T-W5 — warden emits exactly one build-PipelineID-tagged aggregate verdict marker; the red-team leg's marker (warden's own run-id) is NOT surfaced by build's PipelineID filter (F2 / I-W7). Marker is stamped AFTER a fix commit.",
  "reviewer_set_mode": "full",
  "scope": "HEAD~1..HEAD",
  "diff_shape": "NON-security, multi-file — temper is the sole fixer (fixes one Critical by editing an existing line); all other legs clean",
  "per_leg_verdict_vector": {
    "temper": "trips then FIXES to merge-verdict termination in uncommitted mode (warden commits the residual); clean after fix",
    "delve": "clean",
    "red-team": "quality-gate verdict PASS",
    "siege": "NOT RUN — condition-skipped (non-security diff)",
    "inquisitor": "all adversarial tests PASS"
  },
  "_note": "Operator input for the live /warden run. The scorer never reads this file."
}
